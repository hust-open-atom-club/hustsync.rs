[package]
name = "hustsync-config-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1.12.3"
serde = { "version" = "1", features = ["derive"] }
thiserror = "2.0"
toml = "0.9"

[lints]
workspace = true
